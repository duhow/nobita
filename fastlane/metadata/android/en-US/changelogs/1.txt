- Initial Nobita application setup.
- Added local Shizuku-backed Bluetooth HCI capture and PCAPNG export.
- Added paired and nearby device selection plus optional raw BTSnoop retention.
- Added safe full-capture fallback when target filtering finds no packets.
- Fixed startup crashes when connecting to Shizuku.
- Prevented startup crashes when Shizuku authorization is missing.
- Added packet and connection counts to export results.
- Added detected ATT packet counts to export results.
- Battery optimization now opens Nobita's app-specific settings.
- Nearby scanning shows progress and the number of devices found.
- Added a fallback for devices that reject direct Bluetooth snoop property changes.
- Prevented overlapping nearby scans from replacing failure feedback with stale results.
- Added a foreground notification warning when Shizuku stops during capture.
- Added first-launch onboarding for Shizuku, Nearby devices, notifications, and optional battery optimization.
- Stopped changing or restarting Bluetooth during capture setup and cleanup.
- Added a blue capture-active top bar and packet-count feedback.
- Fixed failed exports getting stuck on an unavailable full-capture action.
- Reconciled stale pending sessions when the app reconnects to Shizuku.
- Refreshed capture controls when returning to the app after background export cleanup.
- Preserved recoverable raw captures when the Shizuku service disconnects during export.
- Prevented duplicate export requests while a capture is being processed.
- Fixed duplicate bugreport generation when exporting from the Activity and notification.
- Fixed full capture export failures for controller-to-host packets.
- Showed the processed packet count after full capture export.
- Added visible export progress stages and clearer onboarding permission states.
- Made onboarding permission checks visible in dark mode and fixed their title alignment.
- Fixed export permission errors by staging captures in app-owned storage before publishing them to Downloads.
- Switched capture acquisition from slow bugreport snapshots to live btsnoop_net streaming.
- Kept exports valid when Android BTSnoop records arrive slightly out of timestamp order.
- Surface unexpected live stream interruptions and recoverable packet progress.
- Bound full-capture recovery to the original capture session.
- Exposed capture status through a versioned machine-readable response.
- Prevented recovery checks from selecting another capture session.
- Recovered complete records from interrupted active stream files after service recreation.
- Reconciled interrupted captures after the app or UserService is recreated.
- Preserved one-time conversion for legacy timestamp-named pending captures.
- Made stop and export operations safe against stale or duplicate capture actions.
- Made capture-start responses versioned and machine-readable.
- Refused capture startup when staging storage is unavailable or too low.
- Persisted live packet progress so capture state survives Activity recreation.
- Re-enabled Stop & export for every new capture session.
- Prevented duplicate exports from losing the staged PCAPNG and made generated filenames capture-unique.
- Derived the capture staging package path from the application ID.
- Aligned capture staging with Android’s resolved external-storage path.
- Rebuilt the active capture screen from authoritative UserService state after process recreation.
- Scoped startup-abort cleanup to the capture session that actually started.
- Improved recovery when connecting to the Shizuku capture service.
- Prevented storage recovery errors from blocking the Shizuku UserService connection.
